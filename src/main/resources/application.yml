spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: VelogProject
  datasource:
    url: jdbc:mysql://localhost:3307/Velog
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true

server:
  port: 80
  servlet:
    session:
      timeout: 30m

upload:
  dir: C:\Users\samsung\Desktop\project\img
